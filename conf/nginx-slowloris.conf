# Anti-Slowloris Protection (Managed by DDoS-Protect)
# Drops TCP connections that send HTTP headers or body too slowly

client_body_timeout 5s;
client_header_timeout 5s;
keepalive_timeout 10s;
send_timeout 10s;
